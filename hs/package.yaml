name: ff-cli

# library:
#   exposed: false
#   source-dirs: lib

executables:
  ff:
    dependencies:
      - filepath
      - optparse-applicative
      - persistent-sqlite
      - persistent-template
      - text
      - xdg-basedir
    main: Main.hs
    source-dirs: exe

tests:
  test:
    dependencies:
      - containers
      - process
      - tasty
      - tasty-hunit
      - temporary
      # package
      # - ff-cli
    main: Main.hs
    source-dirs: test

dependencies:
  - base
